import XCTest
@testable import XcodeProjectPlugins

final class XcodeProjectPluginsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(XcodeProjectPlugins().text, "Hello, World!")
    }
}
